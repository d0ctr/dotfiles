[Unit]
Description=Disable USB wake events

[Service]
Type=oneshot
ExecStart=/bin/sh -c "echo XHC0 > /proc/acpi/wakeup && echo XHC1 > /proc/acpi/wakeup && echo XHC2 > /proc/acpi/wakeup && echo XH00 > /proc/acpi/wakeup"

[Install]
WantedBy=multi-user.target
