- defaults:
    link:
      relink: true
      force:  true

- clean: ['~']

- link:
    ~/.config/whkdrc:   config/whkdrc

- create:
    - ~/.config

- shell:
  - [git submodule update --init --recursive, Installing submodules]
